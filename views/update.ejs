<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Blog</title>
    <!-- TailwindCSS -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto py-12">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-8">Update Blog</h1>
        <form action="/update-blog" method="POST" class="bg-white shadow-lg rounded-lg p-6 max-w-lg mx-auto">
            <input type="hidden" name="id" value="<%= blog.id %>">
            <div class="mb-4">
                <label for="title" class="block text-gray-700 font-semibold">Title</label>
                <input type="text" name="title" id="title" value="<%= blog.title %>" 
                    class="w-full mt-2 p-2 border rounded-lg" required>
            </div>
            <div class="mb-4">
                <label for="content" class="block text-gray-700 font-semibold">Content</label>
                <textarea name="content" id="content" rows="6" 
                    class="w-full mt-2 p-2 border rounded-lg" required><%= blog.content %></textarea>
            </div>
            <div class="mb-4">
                <label for="author" class="block text-gray-700 font-semibold">Author</label>
                <input type="text" name="author" id="author" value="<%= blog.author %>" 
                    class="w-full mt-2 p-2 border rounded-lg" required>
            </div>
            <div class="flex justify-end">
                <button type="submit" 
                        class="bg-blue-600 text-white px-4 py-2 rounded-lg shadow hover:bg-blue-500 transition-colors">
                    Update Blog
                </button>
            </div>
        </form>
    </div>
</body>
</html>
